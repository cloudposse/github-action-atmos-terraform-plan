components:
  terraform:
    foobar-atmos-pro:
      metadata:
        component: foobar
      settings:
        pro:
          enabled: true
        github:
          actions_enabled: false
      vars:
        example: blue
        enabled: true
