components:
  terraform:
    foobar/changes:
      component: foobar
      settings:
        github:
          actions_enabled: true
      vars:
        example: blue
        enabled: true
        enable_failure: false
        enable_warning: true
