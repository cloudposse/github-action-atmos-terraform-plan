components:
  terraform:
    foobar/changes/atmos:
      metadata:
        component: foobar
      vars:
        example: blue
        enabled: true
        enable_failure: false
        enable_warning: true
